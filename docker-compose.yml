# Compose file always stays at repo root.
version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    environment:
      CPU_THRESHOLD: 10
      MEMORY_THRESHOLD: 10
